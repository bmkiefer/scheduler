%h1 Create New User

= form_tag users_path do

  = label :user, :username, 'Username'
  = text_field :user, 'username'

  = label :user, :email, 'Email'
  = text_field :user, 'email'

  = label :user, :first_name, 'First Name'
  = text_field :user, 'first_name'

  = label :user, :last_name, 'Last Name'
  = text_field :user, 'last_name'

  = label :user, :password, 'Password'
  = text_field :user, 'password'

  = label :user, :total_score, 'Total Score'
  = text_field :user, 'total_score'

  = label :user, :level_one_flag, 'Level One Flag'
  = text_field :user, 'level_one_flag'

  = label :user, :role,'Role'
  = select :user, :role, ['admin','user']

  = submit_tag 'Save Changes'
